# ==========================================
# Configurações de Ambiente
# ==========================================

# URL da API do Backend
# Em desenvolvimento, pode usar o mock (não definir ou deixar vazio)
# Em produção, definir a URL real do backend
VITE_API_URL=http://localhost:3001/api

# URL da API Admin (usa a mesma URL base)
VITE_ADMIN_API_URL=http://localhost:3001/api

# Usar dados mock (true/false)
# Quando true, usa dados simulados em vez de chamar a API
# Útil para desenvolvimento sem backend
VITE_USE_MOCK=true
